Every iteration calculates the current are as the difference of pointers multiplied by the minimum of the two heights. If the current area is greater than the current max area replace it. Then always move the pointer with the shortest height. Moving the taller height cannot improve the area as it decreases the width and you are still using the shorter height. Therefore moving the larger height doesn't make sense because you want to maximize the heights. 