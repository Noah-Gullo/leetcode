It's helpful to walkthrough the various cases before programming the solution. First if the new interval comes before the current interval append it to result and add remaining intervals. Second, if the current interval comes before the new interval the start of newInterval is greater than current intervals end so it doesn't overlap and we can safetly add it to the result. If the intervals overlap then merge them by getting the minimum of the two and the maximum of the two. After mergine append the new interval to result and return.