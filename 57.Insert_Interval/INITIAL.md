Initial Thoughts:
I need some way of keeping track of the initial interval then figuring out which intervals lie in the new interval. Then I need to remove all intervals between the starting interval and the last interval whose end is less than the end of the new interval. Naive approach is to loop through every interval and keep track of each number and the next number. If the starting end of newInterval is between the number and nextNumber get the index to figure out what the starting interval should be. Keep track of the starting number of that starting interval which will be merged. Then continually loop through all numbers in the next interval and find the interval which should be the end.